# https://taskfile.dev

version: "3"

tasks:
  deploy:
    cmds:
      - docker compose exec aws sh ./deploy.sh

  deploy-moto:
    cmds:
      - docker compose exec aws sh ./deploy-moto.sh

  deploy-all:
    cmds:
      - task: deploy
      - task: deploy-moto
